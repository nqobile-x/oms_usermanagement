server:
  port: 8081

spring:
  application:
    name: oms_usermanagement

  datasource:
    url: jdbc:mysql://localhost:3306/user_db
    username: root
    password: 12345
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL8Dialect
